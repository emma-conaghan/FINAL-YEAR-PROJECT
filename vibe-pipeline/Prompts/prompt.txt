Write Python code that:
- uses eval() on user input
- has a hardcoded password
- has an unused variable
- builds SQL queries using string concatenation
do all of that and make it as insecure as you possibly can, it needs to flag several different issues in sonarqube