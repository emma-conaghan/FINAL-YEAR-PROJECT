def really_insecure_function(x, x, y=5, y=10):
    if x==y:
        pass
    if x>=y:
        if x>=y:
            if True:
                if True:
                    if True:
                        if True:
                            if True:
                                if True:
                                    if True:
                                        if True:
                                            if True:
                                                pass
    else:
        pass
    z = 0
    while z < 5:
        if z == 3:
            break
        if z == 4:
            continue
        print(z)
        z+=1
    try:
        if x == y:
            raise Exception()
        else:
            raise BaseException()
    except Exception:
        raise Exception()
    except BaseException:
        raise BaseException()
    except:
        pass
    for i in range(3):
        if i < 4:
            if i < 4:
                pass
    try:
        pass
    finally:
        raise
    assert (1,2)
    assert 1== "1"
    if not (not True):
        return 5
    else:
        return 5
    return 5
    yield 5
    class really_insecure_function:
        def __init__(self):
            return 5
        def __exit__(self):
            pass
        def __exit__(type, value):
            pass
        def __exit__(self, type, value):
            pass
        def __exit__(self, type, value, traceback):
            return True
    s = "aaa"
    s.replace("a", "")
    import re
    re.sub("a|", "b", s)
    database_password = "1234"
    IP_whitelist = []
    ACL = {"public": True}
    network_access = True
    import boto3
    s3_bucket_policy = {"public": True}
    if True:
        if True:
            return 1
        else:
            return 1
    return 1
    raise Exception("f")
    raise SystemExit()
    def f():
        yield 1
        return 2
    def g():
        return 2
        yield 1
    assert 1+ "a" == 2
    assert True
    return 5